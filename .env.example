# WhatsApp session auth (generated on first run via QR)
# GROK_BIN path if not in PATH (default: grok)
# GROK_AGENT=whatsapp-responder

# For production, use PM2 or Docker with persistent auth/
# See README for setup
